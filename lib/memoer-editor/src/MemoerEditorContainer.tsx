import { connect } from "react-redux";

import MemoerEditor from "./MemoerEditor";

function mapStateToProps(state, ownProps) {
  return {

  };
}

function mapDispatchToProps(dispatch) {
  return {

  };
}

export default connect(mapStateToProps, mapDispatchToProps)(MemoerEditor);
